# 🚀 Deploy: รันบนเครื่องก่อน (ยังไม่ต้อง deploy)

คู่มือสำหรับ **รัน GUI และ Check-in API บนเครื่องแบบปกติ** — เมื่อพร้อมค่อย deploy API ขึ้น server ทีหลัง

---

## ภาพรวม

| ส่วน | ที่รัน | หมายเหตุ |
|------|--------|----------|
| **GUI (Streamlit)** | บนคอมคุณ | รันด้วย `run.bat` หรือ `streamlit run gui_app.py` |
| **Check-in API** | บนคอมคุณ (หรือ server เมื่อ deploy แล้ว) | รันด้วย `uvicorn main:app` จากโฟลเดอร์ `checkin-api` — ไม่ต้อง deploy ก่อน |

---

## 1. รันบนเครื่องแบบปกติ (แนะนำก่อน)

รันทั้ง GUI และ API บนเครื่องคุณ — ไม่ต้องมีบัตร ไม่ต้อง deploy ที่ไหน

### 1.1 รัน Check-in API (เทอร์มินัลแรก)

```bash
cd C:\Users\0355\Desktop\map-main\checkin-api
pip install -r requirements.txt
python -m uvicorn main:app --reload --port 8000
```

ปล่อยเทอร์มินัลนี้ไว้ — API จะรันที่ `http://localhost:8000`

### 1.2 ตั้งค่า .env ใน map-main

ในโฟลเดอร์ **map-main** สร้างหรือแก้ `.env`:

```env
# AI Keywords
GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth (สำหรับส่งอีเมลจาก GUI)
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxx
GOOGLE_REDIRECT_URI=http://localhost:8502/

# Check-in API — รันบนเครื่องใช้ localhost
CHECKIN_API_URL=http://localhost:8000
```

- **CHECKIN_API_URL:** รันบนเครื่องใช้ `http://localhost:8000` (ไม่ต้องใส่ถ้าใช้ค่าเริ่มต้นนี้)

### 1.3 รัน GUI (เทอร์มินัลที่สอง)

ดับเบิลคลิก **run.bat** หรือรัน:

```bash
cd C:\Users\0355\Desktop\map-main
python -m streamlit run gui_app.py --server.port=8502
```

เปิดเบราว์เซอร์ที่ `http://localhost:8502` — หน้า Check-in Dashboard จะเชื่อมกับ API ที่ localhost:8000

### 1.4 Google OAuth (ถ้าจะใช้ส่งอีเมลจาก GUI)

1. เปิด [Google Cloud Console](https://console.cloud.google.com/apis/credentials) → OAuth 2.0 Client ID (Web)
2. ใน **Authorized redirect URIs** เพิ่ม `http://localhost:8502/`
3. ใน OAuth consent screen เพิ่ม Test users (Gmail ที่จะใช้)

---

## 2. เมื่อจะ deploy API ทีหลัง (ยังไม่ต้องทำตอนนี้)

เมื่อต้องการให้ API รันบน server (ไม่ต้องเปิดเทอร์มินัล API บนคอม) สามารถใช้ได้ เช่น

- **Render.com** — ฟรี ไม่ต้องบัตร, deploy จาก Git, มี cold start ตอนหลับ (ดูคู่มือใน `checkin-api/README.md`)
- **VPS (Linux)** — รันด้วย systemd หรือ Docker (รายละเอียดเดิมอยู่ในประวัติของไฟล์นี้)

หลัง deploy แล้ว ตั้งใน `.env` ของ map-main:

```env
CHECKIN_API_URL=https://your-api-url.onrender.com
```

---

## 3. ความปลอดภัย

| รายการ | แนะนำ |
|--------|--------|
| **.env** | ไม่ commit ใน Git |
| **.gmail_oauth.json** | อยู่ใน .gitignore — เก็บเฉพาะบนเครื่องที่รัน GUI |
| **OAuth Client Secret** | เก็บใน .env ไม่ใส่ในโค้ด |

---

## 4. ตรวจสอบหลังรันบนเครื่อง

1. **API:** เปิด `http://localhost:8000/health` ได้และได้สถานะ 200  
2. **GUI:** เปิด `http://localhost:8502` ได้  
3. **Check-in Dashboard:** ขึ้น "✅ เชื่อมต่อ Check-in API สำเร็จ"  
4. **Login Gmail:** ล็อกอินด้วย Google แล้ว redirect กลับมาที่ localhost ได้

---

## สรุป Checklist (รันบนเครื่องก่อน)

- [ ] รัน **Check-in API** จากโฟลเดอร์ `checkin-api` (uvicorn พอร์ต 8000)
- [ ] ตั้ง **CHECKIN_API_URL=http://localhost:8000** ใน `.env` ของ map-main (หรือไม่ใส่ก็ได้)
- [ ] ตั้ง **GOOGLE_REDIRECT_URI=http://localhost:8502/** และเพิ่มใน Google Console (ถ้าจะใช้ส่งอีเมล)
- [ ] รัน **GUI** ด้วย run.bat หรือ `streamlit run gui_app.py --server.port=8502`
- [ ] ไม่ commit `.env` และ `.gmail_oauth.json`

---

## ทดสอบรอบสุดท้าย (ฟังก์ชันใช้งานได้)

| รายการ | วิธีทดสอบ | ผลที่ควรได้ |
|--------|------------|-------------|
| **Check-in API /health** | เปิด `http://localhost:8000/health` | JSON: `{"status":"ok","message":"API is running",...}` |
| **Check-in API /api/tokens** | เปิด `http://localhost:8000/api/tokens` | รายการ token (หรือ `[]`) |
| **Check-in API /api/responses** | เปิด `http://localhost:8000/api/responses` | รายการ interested / unsubscribed |
| **Check-in API สร้าง token** | GUI → Check-in Dashboard → สร้าง Token ใหม่ | ได้ลิงก์ check-in |
| **GUI เชื่อม API** | รัน GUI แล้วไป Check-in Dashboard | ขึ้น "✅ เชื่อมต่อ Check-in API สำเร็จ" |
| **กดลิงก์ Check-in** | เปิดลิงก์ที่ได้จากสร้าง token | หน้า check-in โหลดได้ และกดส่งได้ |
| **ส่งอีเมล (Gmail)** | GUI → Results → Emails → ส่งอีเมล (หลังล็อกอิน Gmail) | ส่งได้และมีลิงก์ check-in |
